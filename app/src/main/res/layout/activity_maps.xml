<RelativeLayout
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"

        tools:context="com.accmxxgmail.shuttlrdemo.MapsActivity" />

    <fragment
        android:layout_margin="20dp"
        android:id="@+id/place_fragment"
        android:name="com.google.android.gms.location.places.ui.PlaceAutocompleteFragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/format_curved_edge_button_wg"/>


    <ImageButton
        android:id="@+id/myMapLocationButton"
        android:layout_alignBottom="@id/map"
        android:layout_alignEnd="@id/map"
        android:layout_alignRight="@id/map"
        android:layout_margin="10dp"
        android:elevation="2dp"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:backgroundTint="@color/colorLightGrey"
        android:onClick="LocateSelf"
        android:src="@drawable/icon_crosshairs_gps"
        android:tint="@color/colorRed"
        />

</RelativeLayout>